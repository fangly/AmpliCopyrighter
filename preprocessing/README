The scripts and data in this folder are useful for producing a file suitable
for Copyrighter input

Overview:
1/ Use the script img_gg_matcher to associate more IMG IDs GG (prokMSA) IDs.
2/ Use the script data_combiner to merge traits from sources like IMG,
Greengenes and GOLD into a single file.
3/ Use the script trait_by_clade_avg (or trait_by_clade) to summarize the trait
at different taxonomic levels.
4/ Use the resulting file in copyrighter.
